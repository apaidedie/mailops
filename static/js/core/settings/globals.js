// split globals from settings.js
// Extracted from main.js lines 7550-9322 (W3 frontend split)
        // ==================== 设置相关 ====================
        // Must be a real binding (not implicit global) so tab switch never throws.
        let currentSettingsTab = 'basic';

        // ==================== 一键更新配置（更新方式切换） ====================

