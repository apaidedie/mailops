# Batch tag/move modals warm soft-paint without loading flash

## Goal
loadTagsForSelect / loadGroupsForBatchMove paint warm caches without 加载中 flash; language soft-paints open batch-move select.

## Acceptance
- [x] warm path return before spinner
- [x] paintBatchMoveGroupSelectFromWarmGroups + softPaintBatchMoveGroupSelectIfOpen
- [x] contracts green
