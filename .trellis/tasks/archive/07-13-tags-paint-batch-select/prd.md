# loadTags paints batch-tag select from warm allTags

## Goal
paintBatchTagSelectFromWarmTags on loadTags soft path + network success; loadTagsForSelect uses it.

## Acceptance
- [ ] helper + call sites
- [ ] contract green
