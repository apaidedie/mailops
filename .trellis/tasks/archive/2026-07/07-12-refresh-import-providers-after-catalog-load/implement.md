# Implementation Plan

1. From catalog success in `main.js`, call `loadProviders(true)` when available.
2. Update settings frontend contract tests.
3. Validate.

## Validation

- Focused unittest
- `node --check static/js/main.js`
- `git diff --check`
