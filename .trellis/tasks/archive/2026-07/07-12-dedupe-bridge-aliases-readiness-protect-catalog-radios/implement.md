# Implementation

1. Add `canonicalizeUnifiedTempProviderKey` + `dedupeUnifiedTempProviderRows` in mailboxes.js; use in readiness list, routing matrix, capability rows.
2. Soft-touch plugins.js inject paths to preserve existing catalog labels.
3. Contract tests.
4. Validate.
