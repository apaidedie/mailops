# Implementation Plan

1. Use `resolveMailboxProviderLabel` in guide/contract/schema render paths in `main.js`.
2. Update settings frontend contract tests.
3. Validate.

## Validation

- Focused unittest
- `node --check static/js/main.js`
- `git diff --check`
